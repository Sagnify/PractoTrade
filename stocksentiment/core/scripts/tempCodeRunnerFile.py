    # print("\nReddit Sentiments:")
    # for post in reddit_posts:
    #     print(f"{post['sentiment_category'].upper()} | {post['sentiment_score']} | {post['title']}")

    # print("\nNews Sentiments:")
    # for article in news_articles:
    #     print(f"{article['sentiment_category'].upper()} | {article['sentiment_score']} | {article['title']}")